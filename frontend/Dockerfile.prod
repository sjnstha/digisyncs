FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci --production=false

COPY . .

# Build for production — output goes to /app/dist
RUN npm run build

# Nginx serves the dist folder via volume
FROM alpine:3.19
COPY --from=builder /app/dist /app/dist
