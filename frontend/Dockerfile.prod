FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci --production=false

COPY . .
RUN npm run build

# ← Fix: copy to correct location nginx expects
FROM alpine:3.19
COPY --from=builder /app/dist /usr/share/nginx/html